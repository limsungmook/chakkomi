<p id="notice"><%= notice %></p>

<div class="talk_form">
<% if user_signed_in? %>
  <% if current_user.name == nil or current_user.name == '' %>
    <%= link_to "프로필", edit_user_registration_path %> 에서 이름을 적으셔야 글을 작성할 수 있습니다.
  <% else %>
    <% if @talk.nil? %>
      <% @talk = Talk.new  %>
    <% end %>
    
    <%= current_user.name %>

      <%= form_for(@talk, :remote => true ) do |f| %>
        <%= f.text_field :content, :class => "defaultText", :title => "착곰이에게 하고 싶은 말을 남겨주세요 ^-^" %>
        <%= f.check_box :secret %>
        <%= f.label :secret %>
      <% end %>
   <% end %>
<% end %>
</div>

<%= render 'talk_talk' %>
