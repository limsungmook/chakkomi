<div id="admin_wrapper">
<%= render :partial => "/admins/admin_menu" %>
<table id="admin_order_table" class="admin_table">
  <tr>
    <th>이름</th>
    <th>예금주</th>
    <th>주소</th>
    <th>Email</th>
    <th>결제수단</th>
    <th>착/선불</th>
    <th>예금액</th>
    <th>입금여부</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @orders.each do |order| %>
  <tr>
    <td><%= order.name %></td>
    <td><%= order.account_owner %></td>
    <td><%= order.address %></td>
    <td><%= order.email %></td>
    <td><%= order.pay_type %></td>
    <td><%= order.arrival_payment %></td>
    <td><%= order.total_price %></td>
    <td><%= order.deposit_check %></td>
    <td><%= link_to '자세히', order %></td>
    <td><%= link_to '수정', edit_order_path(order) %></td>
    <td><%= link_to '삭제', order, :confirm => '정말 삭제하시겠습니까?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />
<%= link_to '이름순 정렬', {:controller => :admins, :action => :order_list, :order => 'name'} %>
<%= link_to '입금순 정렬', {:controller => :admins, :action => :order_list, :order => 'deposit'} %>
<br />
<%= will_paginate @orders %>
<!-- <%= link_to '새로운 주문', new_order_path %>-->
</div>
